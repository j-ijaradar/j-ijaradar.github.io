---
export interface Props {
  name: string;
  description: string;
  tech_stack?: string[];
  status?: string;
  website?: string;
  github?: string;
  docs?: string;
  demo?: string;
  featured?: boolean;
  publications?: Array<{ title: string; url: string }>;
}

const { name, description, tech_stack = [], status, website, github, docs, demo, featured, publications = [] } = Astro.props;

const isPlaceholder = (v?: string) => !v || v.startsWith('[');

const statusStyle =
  status === 'Active Development' ? 'bg-green-100 text-green-800 dark:bg-green-950 dark:text-green-300' :
  status === 'In Development'     ? 'bg-amber-100 text-amber-800 dark:bg-amber-950 dark:text-amber-300' :
  status === 'Planned'            ? 'bg-navy-100 text-navy-600 dark:bg-navy-800 dark:text-navy-400' :
  'bg-primary-100 text-primary-800 dark:bg-primary-950 dark:text-primary-300';
---

<div class:list={['card flex flex-col gap-4', featured && 'ring-1 ring-primary-200 dark:ring-primary-800']}>
  <!-- Header -->
  <div class="flex items-start justify-between gap-3">
    <h3 class="font-semibold text-navy-900 dark:text-navy-50 text-base">{name}</h3>
    {status && (
      <span class={`text-xs font-medium px-2 py-0.5 rounded-full shrink-0 ${statusStyle}`}>
        {status}
      </span>
    )}
  </div>

  <!-- Description -->
  <p class="text-sm text-navy-600 dark:text-navy-300 leading-relaxed -mt-1">{description}</p>

  <!-- Tech stack -->
  {tech_stack.length > 0 && (
    <div class="flex flex-wrap gap-1.5">
      {tech_stack.map((tech: string) => (
        <span class="text-xs font-mono px-2 py-0.5 rounded bg-navy-100 dark:bg-navy-800 text-navy-600 dark:text-navy-400">
          {tech}
        </span>
      ))}
    </div>
  )}

  <!-- Links -->
  {(!isPlaceholder(website) || !isPlaceholder(github) || !isPlaceholder(docs) || !isPlaceholder(demo)) && (
    <div class="pt-3 border-t border-navy-100 dark:border-navy-800 flex flex-wrap gap-4">
      {!isPlaceholder(website) && (
        <a href={website} class="btn-ghost text-xs" target="_blank" rel="noopener">
          <i class="fa-solid fa-arrow-up-right-from-square"></i> Website
        </a>
      )}
      {!isPlaceholder(github) && (
        <a href={github} class="btn-ghost text-xs" target="_blank" rel="noopener">
          <i class="fa-brands fa-github"></i> GitHub
        </a>
      )}
      {!isPlaceholder(docs) && (
        <a href={docs} class="btn-ghost text-xs" target="_blank" rel="noopener">
          <i class="fa-solid fa-book"></i> Docs
        </a>
      )}
      {!isPlaceholder(demo) && (
        <a href={demo} class="btn-ghost text-xs" target="_blank" rel="noopener">
          <i class="fa-solid fa-play"></i> Demo
        </a>
      )}
    </div>
  )}

  <!-- Related Publications -->
  {publications.filter((p: any) => p.url && !p.url.startsWith('[')).length > 0 && (
    <div class="text-xs text-navy-500 dark:text-navy-400">
      <span class="font-medium text-navy-600 dark:text-navy-300">Related:</span>
      <ul class="mt-1 space-y-0.5">
        {publications.filter((p: any) => p.url && !p.url.startsWith('[')).map((pub: any) => (
          <li>
            <a href={pub.url} class="hover:text-primary-700 dark:hover:text-primary-400 hover:underline" target="_blank" rel="noopener">
              {pub.title}
            </a>
          </li>
        ))}
      </ul>
    </div>
  )}
</div>
